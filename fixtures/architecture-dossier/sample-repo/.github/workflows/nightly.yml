name: Nightly digest
on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://app.harbor-ledger.test/api/cron/digest
