import { describe, it, expect } from 'vitest';

describe('contacts', () => {
  it('validates email', () => { expect(1).toBe(1); });
  it('scopes by workspace', () => { expect(1).toBe(1); });
  it.skip('merges duplicates', () => {});
});
